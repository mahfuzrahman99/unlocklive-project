/* eslint-disable react/prop-types */


const ArrowSymble = ({RColor, LColor}) => {
    return (
        <div className={`bg-gradient-to-r from-[${RColor}] to-[${LColor}] h-1 w-10 rounded-lg`}>
            
        </div>
    );
};

export default ArrowSymble;